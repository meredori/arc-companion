// See https://kit.svelte.dev/docs/types#app for information about these interfaces
// for the `App` namespace import types
// and what to do when importing types in .svelte files.
declare global {
  namespace App {
    // interface Error {}
    // interface Locals {}
    // interface PageData {}
    // interface Platform {}
  }
}

export {};
