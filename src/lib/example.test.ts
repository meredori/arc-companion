import { describe, it, expect } from 'vitest';

// Minimal sanity test to ensure the test suite runs.
describe('example sanity', () => {
  it('true equals true', () => {
    expect(true).toBe(true);
  });
});
